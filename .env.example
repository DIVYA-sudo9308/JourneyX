# JourneyX environment (frontend milestone 1)
# The backend/API and database are implemented in later milestones (see
# SOURCE_OF_TRUTH §4.11). These are documented here for continuity.

# Base URL used by the client-side API fetcher (search dropdown, notification
# bell). Defaults to same-origin "/api/v1" when unset.
NEXT_PUBLIC_API_BASE_URL=

# --- Backend (not used by the frontend shell; present for later milestones) ---
# DATABASE_URL=
# DIRECT_URL=
# DEMO_AS_OF=
# INGEST_TOKEN=
