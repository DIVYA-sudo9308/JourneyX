import { TabPlaceholder } from "@/components/customer/tab-placeholder";

export default function CustomerJourneyPage() {
  return (
    <TabPlaceholder
      title="Journey timeline"
      note="The unified, cross-channel journey with pattern annotations (S-04) is implemented in a later milestone."
    />
  );
}
