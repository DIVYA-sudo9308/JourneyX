import { TabPlaceholder } from "@/components/customer/tab-placeholder";

export default function CustomerIdentityPage() {
  return (
    <TabPlaceholder
      title="Identity resolution"
      note="The fragments view, convergence graph, and resolution evidence (S-05) are implemented in a later milestone."
    />
  );
}
